#Ask the input from the user
input_string=input("Enter the string from the user:")
#use for loop to run the length of the string
for i in input_string:
#now reverse the string 
	reverse=input_string[::-1]
#print the reversed string	
print(reverse)